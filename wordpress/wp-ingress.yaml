apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wordpress
spec:
  rules:
    - host: winveli.com
      http:
        paths:
        - backend:
            service:
              name: wordpress
              port:
                number: 80
